# WristChat 实测问题修复计划 v0.1.5(待你核对,未动代码)

> 你反馈的 25 条,每条给出:现状 → 修复方案 → 涉及位置。**本计划未修改任何代码**,核对后再动手。
> 另附:微思应用商店机制分析(第 3/12/13/18/19/20 条)。

---

## A. 崩溃类(第 1、21 条)

### 1. 手机同步:点开 → 卡顿 → 二维码左右被切 → 白屏退回设置
**现状**:点击「手机同步」后弹出二维码弹窗,二维码被切边,随后白屏回到设置页(疑似崩溃/重建)。
**原因分析(待实测日志确认)**:
- 自绘弹窗宽度计算单位混用(px/dp),卡片可能比圆内接区域大 → 边角被圆切(第 15 条同源)
- 崩溃点可能是弹窗内容布局或 ZXing 首次加载,无法静态确定
**修复方案**:
1. **重写 WristDialog 尺寸计算**:卡片宽 = min(圆内接正方形宽 × 0.92, 屏幕宽 − 左右安全边距),单位为 px 统一换算;内容超高时卡片内滚动,**按钮固定可见**
2. 二维码单独做成一张「同步卡片」内嵌在「更新与同步」页面(不是弹窗),避免弹窗生命周期问题;或弹窗尺寸修复后重试
3. **新增全局崩溃日志**:应用内捕获未捕获异常写入 `filesDir/crash.log`(设置→关于→查看崩溃日志),复现后把日志发我,精确定位
**涉及**:WristDialog.kt、CategoryActivity.kt(startSync)、WristChatApp.kt(崩溃捕获)

### 21. 手机同步粘贴聊天 JSON 保存 → 软件重启
**现状**:手机同步页粘贴聊天 JSON 后点保存,App 崩溃重启。
**原因(基本确定)**:SyncServer 的回调跑在**后台线程**,保存后调用了 `rebuild()`(触碰 RecyclerView/UI)→ 触发 `CalledFromWrongThreadException` → 崩溃。
**修复方案**:所有来自 SyncServer 的回调统一 `runOnUiThread` 后再碰 UI;保存逻辑与 UI 刷新分离(IO 线程写数据,主线程刷新)。
**涉及**:SyncServer.kt(回调线程标注)、CategoryActivity.kt(applyConfigJson → 主线程)

---

## B. 表冠/圆屏/视觉(第 2、4、12、13、20 条)

### 2. 表冠还是太快
**现状**:一格基本能滑到底。
**方案**:参照微思的手感(有惯性与缓冲):
- 单格像素 `30 → 16`,单次上限 `36 → 24px`
- `scrollBy` 改为 `smoothScrollBy`(带减速动画,更像微思)
- 手速连续滚动时仍即时响应(多事件合并)
- 灵敏度设置保留,默认 1.0
**涉及**:CrownScroll.kt

### 4. 余额页表冠不滚动
**现状**:余额页用的是 `NestedScrollView` 不是 RecyclerView,而表冠只找 RecyclerView → 找不到就无反应。
**方案**:CrownScroll 支持 `NestedScrollView/ScrollView`(滚动 + 限幅)。聊天页是 RecyclerView 会正常滚动(你可复测)。
**涉及**:CrownScroll.kt

### 12. 侧边滚动指示条(微思同款:滑动显示、停止后自动消失)
**现状**:无。
**微思机制**(dex 证实):Compose 旋转滚动 + 右侧细滚动条,滚动时出现、停止约 1 秒后淡出。
**方案**(原生 View 实现):每个可滚动列表(RecyclerView/NestedScrollView)包一层,右侧叠加一条 2dp 宽圆角细条:位置=当前滚动进度、高度=视口/内容比例;滚动时显示,停止 1.2s 淡出。表冠与触摸滚动都触发。
**涉及**:新增 `ScrollIndicatorLayout.kt` + 三个页面接入

### 13. 顶部圆弧时间显示(微思同款:滑到最顶时从上弹出)
**现状**:无。
**微思机制**:`isImmersiveTimeTextEnabled`(dex 证实)——列表在顶部时,时间从上滑入;离开顶部时滑出。
**方案**:每页顶部加一个居中时间胶囊(系统时间,每分钟刷新):
- 列表滚到顶 → 时间胶囊从上方滑入(带过渡动画)
- 向下滚动离开顶部 → 滑出
- 圆屏安全区内显示
**涉及**:新增 `TimeCapsuleView` + 聊天/余额/设置页接入(负一屏另议,见 Q)

### 20. 圆屏仍轻微切边 + 显示大小调节(微思同款)
**现状**:顶部标题(如「余」字顶部)仍有约 1-2dp 被切;无显示大小调节。
**微思机制**(dex 证实):`displayScale`/`fontScale` + `FontScaleConverter`——整体按比例缩放,圆屏适配跟随。
**方案**:
1. 安全区再加保险:所有顶部/底部栏与列表的圆边缩进统一 +4dp 兜底(基于上次实测"余字顶部仍切")
2. 新增「显示大小」设置(0.9–1.3,步进 0.05,默认 1.0):Activity 的 `attachBaseContext` 里按比例调整 `densityDpi`/`fontScale` → 全局 dp/sp 等比缩放;**圆屏安全区按真实像素计算,任意缩放都自动适配**(原理上天然成立,实测验证)
**涉及**:各 layout margin/padding、新增 ScaleManager + 各 Activity.attachBaseContext

---

## C. 设置 UI(第 3、5、6、14、16、17、22 条)

### 3. 交互项改成长条圆角带边框按钮(微思同款),点击边界清晰
**现状**:设置行无边框,堆在一起看不清边界(第 22 条同源:大块空白区域可点,没有视觉边界)。
**微思机制**:Compose 圆角卡片/OutlinedButton 风格,每项独立成块、有边框。
**方案**:
- 设置行(`item_setting.xml`)与分类菜单行:改为**圆角矩形 + 1dp 描边**卡片,行间留 6dp 空隙,背景与页面底色区分
- 行布局改**两行式**:第一行标题,第二行当前值(小号灰字,可换行)——解决长值挤压(第 5 条)
- 分类菜单同样卡片化
**涉及**:item_setting.xml、item_setting_section.xml、fragment_settings.xml、activity_category.xml、SettingsAdapter

### 5. 服务商切换后 Base URL 消失 + 默认链接不更新 + 长文本不换行
**现状**:
- 切到千问再切回 DeepSeek → Base URL 行空白 + 大片空白
- 默认链接不随服务商更新(只在 baseUrl 为空时才填默认)
**原因**:服务商切换时仅在 baseUrl 为空时写入默认值;切走再切回时保留旧值;值过长时单行挤压产生空白。
**方案**:
1. **切换服务商 = 立即把 Base URL/路径换成该服务商的默认值**(保留"自定义后不覆盖"仅针对自定义模式;或提供"恢复默认"小按钮——默认自动覆盖,推荐)
2. 行内值允许换行(两行式布局,见第 3 条)
3. 填写框初始值使用 `当前值.ifBlank { 服务商默认 }`,不再空白
**涉及**:CategoryActivity(CAT_SERVICE)、item_setting.xml

### 6. 温度 / Top P 旁加提示
**方案**:温度与 TopP 行的值下方加灰字:**「高级参数,不要修改,除非你知道自己在干什么」**;行点击时弹窗内同样提示。
**涉及**:CategoryActivity(CAT_MODEL)

### 14. 删除「仓库」设置项
**方案**:移除 CAT_UPDATE 的「仓库」输入行,仓库硬编码 `XHR666/wristchat`(开源仓库就是它,无需自设)。
**涉及**:CategoryActivity(CAT_UPDATE)、SettingsStore(repoOwner/repoName 保留但不再展示)

### 16. 快捷输入逐条管理(手表不能换行)
**现状**:用多行文本框编辑,手表输入法无法换行 → 不好用。
**方案**:改为**逐条管理**:
- 列表:每条一行(标题=内容前 12 字 + 完整内容),点击 = 编辑该条,长按或右侧按钮 = 删除
- 底部「＋ 添加」行 → 弹输入框(单行)新增
- 数量上限 20 条
**涉及**:CategoryActivity(manageQuickInputs 重做)、新增快捷输入列表布局

### 17. 开启密码流程:点「取消」却弹出「关闭密码?」
**现状**:开密码弹窗点取消 → 又弹"是否关闭密码"。
**原因**:开关行适配器里 `setChecked` 会再次触发监听器 → 递归触发 onToggle(false) → 走关闭确认。
**方案**:SettingsAdapter 绑定开关前先 `setOnCheckedChangeListener(null)`,设完值再挂监听;或加绑定中标志位防重入。
**涉及**:SettingsAdapter.kt

### 22. 大片空白但可点(有按压反馈,无边界)
**方案**:并入第 3 条(卡片化 + 两行式布局),空白区域收窄,可点范围 = 卡片本身。

---

## D. 结构/功能(第 7、9、10、11 条)

### 7. 负一屏(会话历史 + AI 标题)
**现状**:三页 [聊天|余额|设置],会话历史要进设置里看;无 AI 标题。
**方案(较大改动)**:
- 页面扩为 **4 页:[负一屏(会话)|聊天(默认进入)|余额|设置]**,启动定位聊天页
- 负一屏 = 会话列表:每条显示 **AI 自动生成的标题**(新会话首条 AI 回复后,用当前模型生成一句话标题,约几百 token,计入消耗)+ 消息数 + 时间;点击 → 聊天页切换到该会话(草稿保留);左滑/按钮可删除
- 会话上限 20 个不变;标题生成可关闭(设置项,默认开?)
**涉及**:新增 SessionListFragment、ChatViewModel(会话切换 + 自动标题)、MainActivity(4 页)、ChatRepository(标题生成请求)

### 9. 设置是否会丢失?
**现状**:已全部存 SharedPreferences + 文件(会话/记忆/技能),重启保留——**这项已做好**,本次不改。核对时你可实测:改模型/主题 → 杀进程 → 重开。

### 10. 清理全部数据确认加 5 秒倒计时
**方案**:确认弹窗的「确定」初始禁用,显示「5…4…3…2…1」倒计时,归零才可点(防误触)。
**涉及**:WristDialog(新增倒计时能力)、CategoryActivity(清理全部数据)

### 11. 周末峰谷规则 + 倒计时含义
**官方文档核实结论**:"高峰时段为北京时间**周一至周五** 9:00–12:00、14:00–18:00,**其余(含整个周末)为空闲**"。官方未提周末特例。
**现状问题**:代码里带了一段 2026-08-23 之前的周末特例逻辑(历史遗留),且倒计时文案没说清"剩的是当前时段"。
**方案**:
1. 删除周末特例,规则简化为官方原文:仅周一至五 9-12/14-18 为高峰
2. 倒计时文案改明确:高峰时显示「距高峰结束 X」;空闲时显示「距下一个高峰开始 X」,并注明"北京时间"
**涉及**:Pricing.kt、BalanceViewModel.kt

---

## E. 体验细节(第 8、15、23 条)

### 8. 启动白屏约 1 秒
**原因**:Manifest 里 application 主题是「亮色」,启动窗口(Starting Window)用亮色 → 白屏;Activity 运行后才切到用户主题。
**方案**:Manifest 主题改为**纯黑启动主题**(启动窗口黑屏),进入后按用户设置切主题;同时加同色 splash 避免闪变。
**涉及**:themes.xml(新增 Splash 主题)、AndroidManifest.xml

### 15. 弹窗四角被切 + 内容多时按钮可能被顶掉
**方案**(与第 1 条同源):修正 WristDialog 尺寸(px 计算、限制在圆内接正方形内);内容区独立滚动、**按钮行固定在卡片底部不随内容滚动**;卡片高度上限 70% 屏高。
**涉及**:WristDialog.kt、dialog_wrist.xml

### 23. 长 LaTeX 公式可左右滑动,短公式不滑动
**方案**:KaTeX WebView 内 `body { overflow-x: auto }`,长公式(超出气泡宽度)出现横向滚动条,短公式(如 x+y=1)不出现。
**涉及**:KatexWebView.kt(HTML 模板 CSS)

---

## F. 微思分析说明(第 18、19 条)

### 18. "基于 Jetpack Compose Wear 全面重构,原生支持 Material 3 Expressive"是什么意思?
- **Jetpack Compose**:安卓的声明式 UI 框架,代码描述界面而非 XML;**Wear 版**额外提供手表组件(旋转滚动列表、TimeText、表冠手势等)。
- **Material 3 Expressive**:新版 Material 设计语言(大圆角、柔和阴影、动态配色)。
- **对咱们的参考价值**:微思整个 UI 是 Compose 写的,所以圆屏/滚动/时间这些自带支持;咱们是**传统 View**,不必重写,但可以**借鉴它的视觉与交互**(圆角卡片、滚动条、时间胶囊、显示缩放)——就是本计划第 2/3/12/13/20 条的做法。重写 Compose 成本高、体积增、不符"内存优先",不建议。

### 19. "开放源代码许可"是否要写?
- 微思在设置里展示所用开源库的许可(合规要求:使用 Apache/MIT/GPL 等库必须在分发中包含许可文本)。
- 咱们已用:ZXing(Apache-2.0)、Markwon(Apache-2.0)、KaTeX(MIT)、marked(MIT)、AndroidX(Apache-2.0),自身 MIT。
- **方案**:设置→关于 加「开源许可」页:列出上述库 + 各自许可全文/链接(Assets 内置文本,零网络)。**有必要,做**。
- 涉及:新增 OpenSourceLicense 页 + assets/licenses/

---

## G. 待你确认的问题(第 24 条)

1. **负一屏**:自动生成会话标题默认**开**还是**关**?(开=每次新会话首条回复后多一次模型调用,约几百 token)
2. **负一屏位置**:确认页序 [负一屏|聊天|余额|设置] 且启动落在聊天页?(负一屏在聊天左边)
3. **显示大小调节范围**:0.9–1.3 步进 0.05,默认 1.0?重启生效还是即时生效?(即时生效体验好,但每次调要重建界面)
4. **顶部时间胶囊**:聊天/余额/设置三页都加,还是只加聊天页?(参考微思:每页列表顶部都有)
5. **第 1/21 条崩溃**:接受我加"崩溃日志文件 + 关于页查看"吗?复现后把日志发我定位
6. **快捷输入逐条管理**:上限 20 条够吗?
7. **服务商切换自动覆盖 Base URL**:确认"切换服务商就换成该商默认地址"(自定义服务商除外)?

---

## 修复顺序建议

1. 崩溃类(1、21)+ 崩溃日志
2. 表冠/余额滚动(2、4)
3. 设置 UI 卡片化 + 布局(3、5、6、14、16、17、22)
4. 弹窗修复(15)+ 启动白屏(8)
5. 圆屏余量 + 显示缩放(20)
6. 滚动条 + 时间胶囊(12、13)
7. 峰谷规则 + 清理倒计时(10、11)+ LaTeX 横滑(23)
8. 负一屏(7)【最大改动,放最后】
9. 开源许可页(19)

请核对以上方案与优先级;有异议的条目指出后我调整,确认后按此修复并出 v0.1.5。
