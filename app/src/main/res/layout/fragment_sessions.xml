<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/wristBackground"
    android:orientation="vertical">

    <io.github.xhr666.wristchat.ui.common.TimeCapsuleView
        android:id="@+id/timeCapsule"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="2dp"
        android:visibility="invisible" />

    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:layout_marginTop="24dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="8dp"
        android:paddingEnd="8dp">

        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:text="会话"
            android:textColor="?attr/wristText"
            android:textSize="14sp"
            android:textStyle="bold" />

        <Button
            android:id="@+id/btnNew"
            style="@style/HeaderIcon"
            android:text="＋" />
    </LinearLayout>

    <io.github.xhr666.wristchat.ui.common.ScrollIndicatorLayout
        android:id="@+id/indicatorWrap"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingStart="20dp"
            android:paddingEnd="20dp"
            android:paddingTop="8dp"
            android:paddingBottom="16dp"
            android:scrollbars="none" />
    </io.github.xhr666.wristchat.ui.common.ScrollIndicatorLayout>

    <TextView
        android:id="@+id/tvEmpty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:paddingBottom="24dp"
        android:text="暂无会话,去聊天页开始第一段对话吧"
        android:textColor="?attr/wristHint"
        android:textSize="12sp"
        android:visibility="gone" />
</LinearLayout>
